<div class="col-sm-6 col-sm-offset-3">
  <div class="pin-side-menu">
    <div class="pin-side-menu-widget-wrap">

      <div class="pin-login-form-wrapper">
        <h3>Receber email para confirmação da conta</h3>
        <p>Entre com o seu email de acesso. Caso não encontremos o seu cadastro, você pode entrar utilizando sua conta do Facebook ou realizar um cadastro.</p>

        <div class="pin-login-form">

          <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
            <%= devise_error_messages! %>

            <div class="field">
              <%= f.label :email %><br />
              <%= f.email_field :email, autofocus: true, value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), placeholder: "contato@seunegocio.com.br" %>
            </div>

            <div class="actions">
              <%= f.submit "Reenviar instruções para confirmação" %>
            </div>
          <% end %>

          <div class="pin-other-options">
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
