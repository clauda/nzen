<div class="col-sm-6 col-sm-offset-3 col-xs-12">
  <div class="pin-side-menu">
    <div class="pin-side-menu-widget-wrap">

      <div class="pin-login-form-wrapper">
        <h3>Recuperar Senha</h3>
        <p>Entre com seu email que nós iremos recuperar sua senha. Caso não o seu cadastro encontremos, você pode entrar utilizando sua conta do Facebook ou realizar um cadastro.</p>

        <div class="pin-login-form">

        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
            <%= devise_error_messages! %>
            <%= f.hidden_field :reset_password_token %>

            <div class="field">
              <%= f.label :password, "Senha" %>
              <%= f.password_field :password, autofocus: true, autocomplete: "off" %>
            </div>

            <div class="field">
              <%= f.label :password_confirmation, "Confirmação de Senha" %><br />
              <%= f.password_field :password_confirmation, autocomplete: "off" %>
            </div>

            <div class="actions">
              <%= f.submit "Enviar instruções para Recuperação de Senha" %>
            </div>
          <% end %>
        
          <div class="pin-other-options">
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
