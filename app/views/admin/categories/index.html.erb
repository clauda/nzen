<p id="notice"><%= notice %></p>

<h1>Categories</h1>
<%= link_to 'New Category', new_admin_category_path, class: 'btn btn-success pull-right' %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Published</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= link_to category.name, [:admin,category] %></td>
        <td><%= category.published ? 'Sim' : 'Não' %></td>
        <td><%= link_to 'Editar', edit_admin_category_path(category) %></td>
        <td><%= link_to 'Destroy', [:admin,category], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
