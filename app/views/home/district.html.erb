<% if @category
    title("#{@category.name} em #{@district.name}") 
  else
    title(@district.name) 
  end %>
<% seo [
  { property: 'description', content: 'Catalogamos mais de 8 mil lojas e serviços em Natal para facilitar o seu dia a dia. Aqui tem o serviço que você procura.' },
] %>

<div class="row">
  <div class="pin-breadcrumbs col-sm-12">
    <h1>
      <%= link_to "Início", root_path, title: "Pinou Home Page" %> <i class="glyphicon glyphicon-menu-right"></i>  <%= link_to "Bairros & Distritos", districts_path, title: "Todos os Bairros" %> <i class="glyphicon glyphicon-menu-right"></i>
      <%= link_to @district.name, district_path(@district), title: @district.name %> 
      <%= " <i class='glyphicon glyphicon-menu-right'></i> <b>#{link_to @category.name, category_path(@category)}</b>".html_safe if @category %>
    </h1>
  </div>
</div>

  <div class="row">
    <div class="col-md-9">
  
      <div class="col-business">
        <ul>
          <% @services.each do |service| %>
            <li class="business-card">
              <div class="pin-featured-items">
                <%= render partial: 'services/business-card', locals: { service: service } %>
              </div>
            </li>
          <% end %>
        </ul>
      </div>

      <%= paginate @services %>
    </div>
    
    <div class="col-md-3" id="sidebar"></div>
  </div>
