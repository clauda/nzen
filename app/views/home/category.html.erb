<div class="content">
  <div class="row">
    <div class="col-md-12">
      <h4><%= @category.name %></h4><hr />
      <ul>
        <% @category.services.each do |record| %>
          <li><%= link_to record.name, service_path(record) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>